"""
we can apply this using Array but the complexity for this is O(n) because after operation we need to shift the element.
Applying Queue using DLL
    enqueue(element)
    dequeue()
    is_empty()
    front()
    rear()
"""

